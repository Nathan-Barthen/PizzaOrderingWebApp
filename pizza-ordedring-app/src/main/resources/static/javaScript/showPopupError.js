/*
 * Used to show an error, if there is an that redirected them to the home page. 
 */

				
	    var popupElement = document.getElementById("popup");
	    if (popupElement.innerText.includes(".")) {
	    	
	        alert(popupElement.innerText);
	    }
				  
